class Product {
  constructor(id, ownerId, title, author, rating, imageUrl, price) {
    this.id = id;
    this.ownerId = ownerId;
    this.title = title;
    this.author = author;
    this.rating = rating;
    this.imageUrl = imageUrl;
    this.price = price;
  }
}

export default Product;
